#Kotlin
kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx3072M
#Gradle
org.gradle.jvmargs=-Xmx3072M -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.caching=true
#App
# Version de la entrega. La usa packageVersion del MSI y, en CI, el tag y el Release que se
# crean al fusionar a main. Subir aqui en el PR que cierra cada version (MAYOR.MENOR.PARCHE,
# mayor > 0 lo exige el MSI). Fusionar sin tocarla no genera Release nuevo.
imagewatch.version=1.1.0